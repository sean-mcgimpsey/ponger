service:
  port: 8080
  # If the following are uncommented, then the server will operate over TLS
  tlsCertificate: /usr/secret/server.crt
  tlsPrivateKey: /usr/secret/server.key

metrics:
  port: 2112
  endpoint: "/metrics"
